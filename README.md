# Twitter-Timeline-Linked-List
The Twitter Timeline Linked List Project simulates how tweets are managed in a feed using linked lists. It combines `singlelinklist.h` for singly linked list operations, with `doublelinklist.h` for doubly linked list features such as two‑way navigation and efficient deletion, merged into a menu‑driven main program.
